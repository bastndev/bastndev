name: Latest YouTube Video
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Generate YouTube cards with duration filter
        run: |
          cat << 'EOF' > generate-youtube-cards.js
          const https = require('https');
          const fs = require('fs');
          
          // CONFIGURACIÓN
          const CONFIG = {
            CHANNEL_ID: 'UC1SrBBxff-ONko-0QKL1Bng',
            MIN_DURATION_MINUTES: 3,
            MAX_VIDEOS_TO_SHOW: 3,
            MAX_VIDEOS_TO_SEARCH: 20
          };
          
          function makeRequest(url) {
            return new Promise((resolve, reject) => {
              const request = https.get(url, (res) => {
                let data = '';
                res.on('data', chunk => data += chunk);
                res.on('end', () => {
                  try {
                    const result = JSON.parse(data);
                    if (result.error) {
                      reject(new Error(`API Error: ${result.error.message}`));
                    } else {
                      resolve(result);
                    }
                  } catch (e) {
                    reject(new Error(`Parse Error: ${e.message}`));
                  }
                });
              });
              
              request.on('error', (err) => {
                reject(new Error(`Request Error: ${err.message}`));
              });
              
              request.setTimeout(10000, () => {
                request.destroy();
                reject(new Error('Request timeout'));
              });
            });
          }
          
          async function generateYouTubeCards() {
            try {
              const API_KEY = process.env.YOUTUBE_API_KEY;
              
              if (!API_KEY) {
                throw new Error('YOUTUBE_API_KEY no está configurada');
              }
              
              const MIN_DURATION_SECONDS = CONFIG.MIN_DURATION_MINUTES * 60;
              
              console.log(`🔍 Buscando videos de ${CONFIG.MIN_DURATION_MINUTES}+ minutos del canal ${CONFIG.CHANNEL_ID}`);
              
              // Obtener videos del canal
              const searchUrl = `https://www.googleapis.com/youtube/v3/search?key=${API_KEY}&channelId=${CONFIG.CHANNEL_ID}&part=snippet&order=date&type=video&maxResults=${CONFIG.MAX_VIDEOS_TO_SEARCH}`;
              console.log('📡 Obteniendo lista de videos...');
              const searchData = await makeRequest(searchUrl);
              
              if (!searchData.items || searchData.items.length === 0) {
                console.log('❌ No se encontraron videos en el canal');
                return;
              }
              
              console.log(`✅ Encontrados ${searchData.items.length} videos`);
              
              // Obtener detalles de duración
              const videoIds = searchData.items.map(item => item.id.videoId).join(',');
              const detailsUrl = `https://www.googleapis.com/youtube/v3/videos?key=${API_KEY}&id=${videoIds}&part=contentDetails,snippet,statistics`;
              console.log('📊 Obteniendo detalles de duración...');
              const detailsData = await makeRequest(detailsUrl);
              
              // Función para convertir duración ISO 8601 a segundos
              function parseISO8601Duration(duration) {
                try {
                  const match = duration.match(/PT(?:(\d+)H)?(?:(\d+)M)?(?:(\d+)S)?/);
                  if (!match) return 0;
                  const hours = parseInt(match[1] || 0);
                  const minutes = parseInt(match[2] || 0);
                  const seconds = parseInt(match[3] || 0);
                  return hours * 3600 + minutes * 60 + seconds;
                } catch (e) {
                  return 0;
                }
              }
              
              // Función para formatear duración
              function formatDuration(seconds) {
                const hours = Math.floor(seconds / 3600);
                const minutes = Math.floor((seconds % 3600) / 60);
                const secs = seconds % 60;
                
                if (hours > 0) {
                  return `${hours}:${minutes.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
                }
                return `${minutes}:${secs.toString().padStart(2, '0')}`;
              }
              
              // Función para formatear número de vistas
              function formatViews(views) {
                try {
                  const num = parseInt(views || 0);
                  if (num >= 1000000) {
                    return (num / 1000000).toFixed(1) + 'M';
                  } else if (num >= 1000) {
                    return (num / 1000).toFixed(1) + 'K';
                  }
                  return num.toString();
                } catch (e) {
                  return '0';
                }
              }
              
              // Función para calcular tiempo transcurrido
              function getTimeAgo(publishedAt) {
                try {
                  const now = new Date();
                  const published = new Date(publishedAt);
                  const diffMs = now - published;
                  const diffDays = Math.floor(diffMs / (1000 * 60 * 60 * 24));
                  const diffMonths = Math.floor(diffDays / 30);
                  
                  if (diffMonths > 0) return `${diffMonths} month${diffMonths > 1 ? 's' : ''} ago`;
                  if (diffDays > 0) return `${diffDays} day${diffDays > 1 ? 's' : ''} ago`;
                  return 'Today';
                } catch (e) {
                  return 'Recently';
                }
              }
              
              console.log('\n📝 Analizando duración de videos:');
              
              // Filtrar videos por duración mínima
              const filteredVideos = detailsData.items
                .filter(video => {
                  const durationInSeconds = parseISO8601Duration(video.contentDetails.duration);
                  const durationInMinutes = Math.floor(durationInSeconds / 60);
                  const status = durationInSeconds >= MIN_DURATION_SECONDS ? '✅ INCLUIDO' : '❌ EXCLUIDO';
                  
                  console.log(`   "${video.snippet.title.substring(0, 50)}..." - ${durationInMinutes}:${(durationInSeconds % 60).toString().padStart(2, '0')} - ${status}`);
                  
                  return durationInSeconds >= MIN_DURATION_SECONDS;
                })
                .slice(0, CONFIG.MAX_VIDEOS_TO_SHOW);
              
              console.log(`\n🎯 Videos finales que cumplen criterio: ${filteredVideos.length}/${CONFIG.MAX_VIDEOS_TO_SHOW}`);
              
              if (filteredVideos.length === 0) {
                console.log('⚠️ No hay videos que cumplan el criterio de duración mínima');
                console.log('💡 Considera reducir MIN_DURATION_MINUTES en la configuración');
                return;
              }
              
              // Generar contenido para README
              let cardsContent = '';
              
              filteredVideos.forEach((video, index) => {
                const durationSeconds = parseISO8601Duration(video.contentDetails.duration);
                const formattedDuration = formatDuration(durationSeconds);
                const formattedViews = formatViews(video.statistics.viewCount);
                const timeAgo = getTimeAgo(video.snippet.publishedAt);
                const title = video.snippet.title.replace(/[<>&"']/g, (char) => {
                  const entities = { '<': '&lt;', '>': '&gt;', '&': '&amp;', '"': '&quot;', "'": '&#39;' };
                  return entities[char];
                });
                const thumbnail = video.snippet.thumbnails.medium?.url || video.snippet.thumbnails.default.url;
                
                cardsContent += `
<a href="https://www.youtube.com/watch?v=${video.id}">
  <img src="${thumbnail}" alt="${title}" width="300">
  <br>
  <strong>${title}</strong>
  <br>
  ${formattedViews} views • ${timeAgo}
  <br>
  ⏱️ ${formattedDuration}
</a>

${index < filteredVideos.length - 1 ? '<br>' : ''}`;
              });
              
              // Leer README actual
              let readmeContent = '';
              try {
                readmeContent = fs.readFileSync('README.md', 'utf8');
                console.log('📖 README.md leído correctamente');
              } catch (e) {
                console.log('📝 README.md no encontrado, creando estructura básica');
                readmeContent = `# Mi Canal de YouTube

## 🔴 Latest videos Youtube

<!-- BEGIN YOUTUBE-CARDS -->
<!-- END YOUTUBE-CARDS -->
`;
              }
              
              // Buscar patrones de comentarios
              const patterns = [
                { start: '<!-- BEGIN YOUTUBE-CARDS -->', end: '<!-- END YOUTUBE-CARDS -->' },
                { start: '<!-- YOUTUBE-CARDS:START -->', end: '<!-- YOUTUBE-CARDS:END -->' }
              ];
              
              let updated = false;
              
              for (const pattern of patterns) {
                const startIndex = readmeContent.indexOf(pattern.start);
                const endIndex = readmeContent.indexOf(pattern.end);
                
                if (startIndex !== -1 && endIndex !== -1) {
                  console.log(`🔄 Actualizando contenido usando patrón: ${pattern.start}`);
                  
                  const newContent = 
                    readmeContent.substring(0, startIndex + pattern.start.length) +
                    '\n' + cardsContent + '\n' +
                    readmeContent.substring(endIndex);
                  
                  fs.writeFileSync('README.md', newContent);
                  console.log('✅ README.md actualizado exitosamente');
                  updated = true;
                  break;
                }
              }
              
              if (!updated) {
                console.log('❌ No se encontraron patrones de comentarios en README.md');
                console.log('📋 Patrones buscados:');
                patterns.forEach(p => console.log(`   ${p.start} ... ${p.end}`));
                
                // Crear archivo de respaldo con el contenido generado
                fs.writeFileSync('youtube-cards-backup.md', cardsContent);
                console.log('💾 Contenido guardado en youtube-cards-backup.md');
              }
              
            } catch (error) {
              console.error('❌ Error durante la ejecución:', error.message);
              
              if (error.message.includes('403')) {
                console.error('🔑 Problema con la API Key:');
                console.error('   - Verifica que YOUTUBE_API_KEY esté configurada');
                console.error('   - Asegúrate de que la API Key tenga permisos para YouTube Data API v3');
                console.error('   - Revisa las cuotas de la API en Google Cloud Console');
              } else if (error.message.includes('404')) {
                console.error('🔍 Canal no encontrado:');
                console.error(`   - Verifica el CHANNEL_ID: ${CONFIG.CHANNEL_ID}`);
              }
              
              process.exit(1);
            }
          }
          
          // Ejecutar función principal
          console.log('🚀 Iniciando generación de tarjetas de YouTube...');
          generateYouTubeCards();
          EOF
          
          node generate-youtube-cards.js
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
      
      - name: Commit and push if changed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Verificar si hay cambios
          if git diff --quiet HEAD README.md; then
            echo "📋 No hay cambios en README.md"
          else
            echo "📝 Hay cambios, realizando commit..."
            git add README.md
            git commit -m "📺 Update YouTube cards (videos 3+ minutes only) - $(date '+%Y-%m-%d %H:%M')"
            git push
            echo "✅ Cambios enviados exitosamente"
          fi